# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env overrides. `.env` itself is tracked and holds only the public
# VITE_ values that the build needs; anything genuinely secret belongs in
# Supabase Edge Function secrets, never in a file here.
.env.local
.env.*.local
